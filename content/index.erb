---
title: What files ignore in git?
---
<div class="jumbotron">
  <h1>.gitignore files for your project!</h1>
  <form>
    <fieldset>
      <p><input id="search" type="text" placeholder="Enter technology…" data-provide="typeahead" data-source="[<%= articles.select { |i| i[:tags].join(', ') }.join(', ') %>]" data-items="4" autocomplete="off"/></p>
      <p><button type="submit" class="btn btn-large btn-success"><i class="icon-search icon-white"></i> Search</button></p>
    </fieldset>
  </form>
</div>


<table class="table table-hover">
  <thead>
    <tr><td>Name</td><td>Tags</td><td>Raw</td></tr>
  </thead>
  
  <% sorted_articles.each do |article| %>
    <tr>
      <td><%= link_to article[:title], article.path %></td>
      <td><%= article[:tags].join(', ') %></td>
      <td><a href="<%= article.path + '.gitignore' %>" download>Download</td>
    </tr>
  <% end %>
  
</table>

<script>
$(document).ready(function() {
  $("#search").focus();
});
</script>
